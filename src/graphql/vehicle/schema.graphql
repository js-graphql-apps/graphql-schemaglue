
type Vehicle {
  id: ID!
  type: String!
  brand: String!
  model: String
  description: String
}


type Query {
  # ### GET vehicles
  #
  # - **id**: Vehicle id (optional)
  vehicles(id: Int): [Vehicle]
}